import { PayloadAction } from "@reduxjs/toolkit";

// ===========================================================
export type setMenuVisibleAction = PayloadAction<boolean>;
// ===========================================================
export type setMenuOpenAction = PayloadAction<boolean>;
